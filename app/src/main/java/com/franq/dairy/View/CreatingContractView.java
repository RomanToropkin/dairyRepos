package com.franq.dairy.View;

import android.view.View;

public interface CreatingContractView {

    void buttonClick(View view);
    boolean checkTitle(String title);
    boolean checkText(String text);
}
